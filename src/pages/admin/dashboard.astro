---
import Layout from '../../layouts/Layout.astro';
import { Settings, LogOut, Save } from 'lucide-react';
---

<Layout title="Nodo Admin - Dashboard">
  <div class="min-h-screen bg-gray-50 flex">
    <!-- Sidebar -->
    <aside class="w-64 bg-nodo-dark text-white hidden md:block fixed h-full">
      <div class="p-6">
        <span class="text-2xl font-bold text-nodo-purple">n<span class="text-white">odo</span></span>
      </div>
      <nav class="mt-6">
        <a href="#" class="flex items-center gap-3 px-6 py-3 bg-nodo-purple/10 text-nodo-purple border-r-4 border-nodo-purple">
          <Settings size={20} />
          <span class="font-medium">Precios y Planes</span>
        </a>
      </nav>
      <div class="absolute bottom-0 w-64 p-6 border-t border-gray-800">
        <a href="/admin" class="flex items-center gap-3 text-gray-400 hover:text-white transition-colors">
          <LogOut size={20} />
          <span>Cerrar Sesión</span>
        </a>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-8 md:ml-64">
      <div class="max-w-4xl mx-auto">
        <header class="flex justify-between items-center mb-8">
          <h1 class="text-2xl font-bold text-gray-900">Gestión de Precios</h1>
        </header>

        <!-- Alert for Demo -->
        <div class="bg-blue-50 border-l-4 border-blue-500 p-4 mb-8 rounded-r-lg">
            <div class="flex">
                <div class="flex-shrink-0">
                    <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                    </svg>
                </div>
                <div class="ml-3">
                    <p class="text-sm text-blue-700">
                        Esta es una interfaz de demostración. Para guardar los cambios, se requiere integración con el API backend.
                    </p>
                </div>
            </div>
        </div>

        <!-- Planes Section -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-8">
          <h2 class="text-lg font-bold text-gray-800 mb-4 border-b pb-2">Planes de Licencia</h2>
          <div class="space-y-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 items-end">
              <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-600 mb-1">Nombre del Plan</label>
                <input type="text" value="Licencia Anual" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-600 mb-1">Precio</label>
                <input type="text" value="$600.000" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
              </div>
            </div>
             <div class="grid grid-cols-1 md:grid-cols-3 gap-4 items-end">
              <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-600 mb-1">Nombre del Plan</label>
                <input type="text" value="Licencia + Equipos" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-600 mb-1">Precio</label>
                <input type="text" value="Consultar" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
              </div>
            </div>
          </div>
        </div>

        <!-- Folios Section -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-8">
          <h2 class="text-lg font-bold text-gray-800 mb-4 border-b pb-2">Paquetes de Folios</h2>
          <div class="space-y-4">
            <div class="grid grid-cols-3 gap-4 items-center p-3 bg-gray-50 rounded-lg">
              <div class="font-medium text-gray-700">500 Folios</div>
              <input type="text" value="$50.000" class="col-span-2 px-3 py-2 bg-white border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
            </div>
            <div class="grid grid-cols-3 gap-4 items-center p-3 bg-gray-50 rounded-lg">
              <div class="font-medium text-gray-700">1.000 Folios</div>
              <input type="text" value="$80.000" class="col-span-2 px-3 py-2 bg-white border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
            </div>
            <div class="grid grid-cols-3 gap-4 items-center p-3 bg-gray-50 rounded-lg">
              <div class="font-medium text-gray-700">5.000 Folios</div>
              <input type="text" value="$300.000" class="col-span-2 px-3 py-2 bg-white border border-gray-200 rounded-lg focus:ring-2 focus:ring-nodo-purple outline-none" />
            </div>
          </div>
        </div>

        <div class="flex justify-end">
          <button class="flex items-center gap-2 px-6 py-3 bg-nodo-purple text-white font-bold rounded-lg hover:bg-purple-700 transition-colors shadow-lg">
            <Save size={20} />
            Guardar Cambios
          </button>
        </div>
      </div>
    </main>
  </div>
</Layout>
